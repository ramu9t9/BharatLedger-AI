# API base URL (empty = same origin; use http://localhost:8000 if frontend runs on different port without proxy)
VITE_API_BASE_URL=

# API timeout in milliseconds (default: 10000)
VITE_API_TIMEOUT=10000
